
package javacore.collection.project.entity;

/**
 * enum là 1 dạng đặc biệt của class, nó chủ yếu lưu trữ tập dữ liệu dạng liệt kê có giới hạn
 * khi nào ta biết được giới hạn của trường hợp thì mới nên dùng enum (vd: Teacher được giới hạn gồm Bachelor, master, doctor)
 * @author HS
 */
//enumerate degree level of a teacher
public enum EDegree {
    BACHELOR, MASTER, DOCTOR
}
